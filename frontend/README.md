# Frontend (Reflex)

Este diretório é um placeholder para o app Reflex.
Comece com: login, dashboard de limites, tela de documentos e chat.

⚠️ CSP em modo **enforce**: todos os assets devem ser servidos de `self` (mais `ws/wss` e `data:` para imagens). Evite CDN externas
ou imports inline de scripts adicionais.
